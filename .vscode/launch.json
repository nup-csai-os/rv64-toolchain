{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach gdb",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/os2025/kernel/kernel",
            "miDebuggerPath": "/usr/bin/gdb-multiarch",
            "MIMode": "gdb",
            "setupCommands": [
                { "text": "target remote localhost:25000" },
            ],
            "cwd": "${workspaceFolder}",
            "externalConsole": false
        }
    ]
}